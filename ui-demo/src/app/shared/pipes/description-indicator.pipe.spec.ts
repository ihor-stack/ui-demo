import { DescriptionIndicatorPipe } from './description-indicator.pipe';

describe('DescriptionIndicatorPipe', () => {
  it('create an instance', () => {
    const pipe = new DescriptionIndicatorPipe();
    expect(pipe).toBeTruthy();
  });
});
